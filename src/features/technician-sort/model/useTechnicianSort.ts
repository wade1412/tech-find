//Read sortMode from URL and pass it to sortTechncians and returned sorted array

import type { Technician } from "../../../entities/technician/technician.types";

export const useTechcnicianSort = (technicians: Technician[]) => {
  return technicians;
};
